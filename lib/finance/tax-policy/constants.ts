/** Standard POS output VAT tax code for AS / ASAS retail sales. */
export const VAT_OUTPUT_STANDARD_TAX_CODE = "VAT_OUTPUT_STANDARD"

/** Default seeded rate: 7.00% inclusive (700 basis points). */
export const DEFAULT_VAT_OUTPUT_STANDARD_RATE_BPS = 700

/** Earliest effective date for the default AS VAT policy seed. */
export const DEFAULT_VAT_POLICY_EFFECTIVE_FROM = "2026-01-01"
