/** Branch / Staff session banner — orange frame matching scan input border weight. */
export const POS_SESSION_BANNER_BORDER_CLASS = "border-2 border-orange-600"

/** Outer shell shared by POS keypad panel (padded interior). */
export const POS_PANEL_FRAME_CLASS =
  "rounded-xl border border-zinc-500 p-3 shadow-inner"

/** Cart panel shell — border/radius only; orange fill extends to inner edges. */
export const POS_CART_PANEL_FRAME_CLASS =
  "rounded-xl border border-zinc-500 shadow-inner overflow-hidden"

/** Horizontal gap between keypad workspace and cart panel (12–16px). */
export const POS_WORKSPACE_GAP_CLASS = "gap-4"
