/** Shared screen layout props for posted finance document detail pages (MJV golden template). */
export const financePostedDocumentScreenProps = {
  embeddedInDocumentContainer: true,
  compactScreenHeader: true,
  showListBackLink: false,
  showPrintActions: false,
  showArchiveDownload: false,
  compactArchiveActions: true,
} as const
