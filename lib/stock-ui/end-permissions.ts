/** UI-facing re-exports — direct path into client-safe domain helpers (not the end barrel). */
export {
  canViewEnd,
  canRebuildEnd,
  canImportEnd,
  canLockEnd,
  canSubmitEnd,
  canReopenEnd,
  canConfirmShopReceipt,
} from "@/lib/stock/end/end-permissions"
